{
  "name": "server",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "packageManager": "yarn@3.2.3",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "dev": "nodemon",
    "start": "yarn build && node dist/index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}
